function checkvlresidual(form) {
 var password = form[form.id + ":password"].value;

 if (valor_residual => 0 && valor_residual <=100)
 form.submit();
 else
 alert("Digite a porcentagem de 0 a 100");
}